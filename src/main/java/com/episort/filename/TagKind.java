package com.episort.filename;


/** Category of a release "noise" tag found inside a file name. */
public enum TagKind {
    RESOLUTION,
    SOURCE,
    CODEC,
    AUDIO,
    DYNAMIC_RANGE,
    EDITION,
    LANGUAGE,
    CHECKSUM,
    WEBSITE,
    GROUP,
    PART,
    EXTRA
}
