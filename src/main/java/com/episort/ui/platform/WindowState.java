package com.episort.ui.platform;

/** The explicit placement states supported by Episort's custom window chrome. */
public enum WindowState {
    NORMAL,
    MAXIMIZED,
    SNAPPED_LEFT,
    SNAPPED_RIGHT
}
